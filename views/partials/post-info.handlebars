<article class="card">
    <a href="/post/{{id}}"> 
        <div class="card-header">
            {{title}} <span class="publish-date">Published by {{user.username}} on {{format_date created_at}}</span>
        </div>
        <div class="card-body">
            {{post_content}}
        </div>
    </a> 
</article>